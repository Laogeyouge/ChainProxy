# Build / dist
dist/
build/
*.dmg
*.spec.bak

# Python bytecode (cross-platform)
__pycache__/

# Windows build artifacts
*.exe.manifest

# Bundled mihomo binary — fetched by build_windows.ps1, not committed
# (35MB, third-party, version-specific; redistribution licence concerns)
scripts/mihomo.exe

# Generated icon — regenerated by make_icon_windows.py, fine to ignore but
# we keep it tracked so the repo always has a valid icon for the spec.
# (uncomment below if you want to generate on each build)
# icon.ico

# The app bundle has source stamped in by scripts/build.sh — don't commit those copies.
ChainProxy.app/Contents/Resources/chainproxy_qt.py
ChainProxy.app/Contents/Resources/chainproxy_core.py
ChainProxy.app/Contents/Resources/core/

# macOS / Python cruft
.DS_Store
.metadata_never_index
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Editor state
.idea/
.vscode/
*.swp
*~

# Claude Code workspace state
.claude/

# Local-only runtime
runtime/
*.log

# User's real config — never commit. Use config.example.json as the template.
config.json
my-config.json
